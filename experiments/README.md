# Phase 0 experiments

These are bounded feasibility experiments, not production renderer contracts.
Permanent orchestration, schemas, environment capture, verification, and result
handling live in `crates/phase0-common` and `crates/xtask`.

