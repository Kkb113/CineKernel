# Web compatibility boundary

Remotion and HyperFrames run as unmodified package baselines. CineKernel wraps
their command boundaries and verifies final artifacts; it does not patch either
framework to improve Phase 0 measurements.

