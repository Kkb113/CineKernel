# Generated fixtures

Run `pnpm fixtures`. The generator writes all assets and their SHA-256 manifest
to `.cinekernel/generated/fixtures/`. Large source videos and audio are never
tracked. The only third-party fixture is Inter Regular from
`@fontsource/inter@5.2.8` under SIL OFL 1.1; it is copied to runtime storage.

