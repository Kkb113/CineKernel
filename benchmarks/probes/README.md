# Correctness probes

Run `pnpm --filter @cinekernel/phase0-common probes`. The runner never deletes
failed evidence. PASS, FAIL, NOT REPRODUCED, UNSUPPORTED, and BLOCKED are the only
allowed states. OS-level network isolation and engine concurrency matrices are
separate, explicit runs; a static local-URL audit does not masquerade as those.

