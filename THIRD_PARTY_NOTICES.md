# Third-party notices

CineKernel Phase 0 evaluates Remotion and HyperFrames as unmodified external
dependencies and studies sparse, generated source checkouts. Their licenses are
preserved in `.cinekernel/upstreams/` during acquisition and are not relicensed
by CineKernel. See `docs/source-lineage/` and `benchmarks/upstreams.lock.json`.

Rust and JavaScript dependencies retain their upstream licenses. Exact versions
are recorded in `Cargo.lock` and `pnpm-lock.yaml`.

